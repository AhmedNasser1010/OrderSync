"use client";

const CheckoutPageTitle = ({ title }: { title: string }) => {
  return (
    <h2 className="text-[35px] font-black tracking-[2px] mb-[50px] text-center">
      {title}
    </h2>
  );
};

export default CheckoutPageTitle;
