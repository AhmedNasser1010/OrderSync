import {
  DialogHeader,
  DialogTitle,
} from "@/components/ui/dialog";

export default function AssignDialogHeader() {
  return (
    <DialogHeader>
      <DialogTitle>Assign Driver to Order</DialogTitle>
    </DialogHeader>
  );
}
